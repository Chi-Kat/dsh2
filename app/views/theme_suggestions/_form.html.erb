<%= form_for(@theme_suggestion) do |f| %>
  <% if @theme_suggestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theme_suggestion.errors.count, "error") %> prohibited this theme_suggestion from being saved:</h2>

      <ul>
      <% @theme_suggestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
